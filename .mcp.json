{
  "mcpServers": {
    "cts": {
      "command": "uv",
      "args": [
        "--directory",
        "${CLAUDE_PLUGIN_ROOT}",
        "run",
        "python",
        "-m",
        "src.server"
      ],
      "env": {
        "CTS_REPO_PATH": "${PWD}"
      }
    }
  }
}
